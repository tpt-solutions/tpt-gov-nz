import RenewVehicleRegistrationForm from "./form";

export const metadata = { title: "Renew Vehicle Registration — Transport — My Gov NZ" };

export default function RenewVehicleRegistrationPage() {
  return <RenewVehicleRegistrationForm />;
}
