import PassportRenewalForm from "./form";

export const metadata = { title: "Renew Passport — Internal Affairs — My Gov NZ" };

export default function PassportRenewalPage() {
  return <PassportRenewalForm />;
}
